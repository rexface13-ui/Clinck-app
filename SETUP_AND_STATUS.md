# DentaFlow — دليل التشغيل + حالة المشروع + الخطوات القادمة

آخر تحديث: 22/07/2026

---

## 1. متطلبات التشغيل (على أي جهاز جديد)

### البرامج المطلوبة مسبقاً على الجهاز
- **PostgreSQL 18** (أو أي نسخة 15+) — مثبّت ويعمل كخدمة
- **Node.js** (نسخة 20+) مع npm
- **Git**

> **ملاحظة**: PHP 8.3 و Composer **لا يلزم تثبيتهم على النظام** — المشروع يحمل نسخته الخاصة محلياً (راجع الخطوة 3). هذا كان قصدياً لأن الجهاز الأصلي فيه PHP 8.2 بس عبر XAMPP، ولارافيل 13 يحتاج 8.3+.

### خطوات التنزيل والتشغيل

**1. Clone المشروع**
```bash
git clone https://github.com/rexface13-ui/Clinck-app.git DentaFlow
cd DentaFlow
```

**2. حمّل PHP 8.3 محلياً (إذا ما كان عندك PHP 8.3+ على الجهاز)**
- نزّل PHP 8.3.32 (النسخة "Thread Safe" أو "Non Thread Safe" حسب الحاجة، x64) من https://windows.php.net/download/
- فك الضغط داخل مجلد `php83/` بجذر المشروع (يعني `DentaFlow/php83/php.exe` لازم يكون موجود)
- حمّل `composer.phar` من https://getcomposer.org/composer.phar وحطه بجذر المشروع (`DentaFlow/composer.phar`)

> إذا عندك PHP 8.3+ وComposer مثبتين عالجهاز أصلاً، تقدر تستخدمهم مباشرة بدل `../php83/php.exe` و`../composer.phar` بكل الأوامر تحت — بس خلي بالك إذا عندك أكتر من نسخة PHP لازم تتأكد إنه بيستخدم 8.3+.

**3. جهّز قاعدة البيانات**
- افتح PostgreSQL وأنشئ قاعدة بيانات اسمها `dentaflow`:
```sql
CREATE DATABASE dentaflow;
```

**4. الباك اند (Laravel)**
```bash
cd backend
../php83/php.exe ../composer.phar install
copy .env.example .env        # (أو cp على Mac/Linux)
```
افتح `backend/.env` وعدّل:
```
DB_CONNECTION=pgsql
DB_HOST=127.0.0.1
DB_PORT=5432
DB_DATABASE=dentaflow
DB_USERNAME=<اسم المستخدم عندك>
DB_PASSWORD=<الباسورد عندك>
```
بعدين:
```bash
../php83/php.exe artisan key:generate
../php83/php.exe artisan migrate --force
../php83/php.exe artisan db:seed --force
../php83/php.exe artisan serve --port=8010
```
هيك الباك اند شغال على `http://127.0.0.1:8010`.

**5. الفرونت اند (React)**
بترمنال تاني:
```bash
cd frontend
npm install
npm run dev -- --port 5183
```
هيك الموقع شغال على `http://localhost:5183`.

**6. تسجيل الدخول الأول**
بيانات المالك (owner) اللي انزرعت من الـ seeder موجودة بملف `backend/database/seeders/ClinicSeeder.php` — افتحه وشوف الإيميل/الباسورد المبدئي (أو غيّرهم هناك قبل ما تشغل `db:seed`).

### ملاحظة تقنية مهمة (Windows فقط)
أي عملية Laravel تشغّل برنامج خارجي (`pg_dump`, `pg_restore`) لازم تمر عبر `App\Support\ProcessEnv::withOverrides()` — لأن `Process::env()` بلارافيل بيستبدل environment variables كامل بدل ما يدمجها، وهاد بيكسر أي native binary على ويندوز (PATH/TEMP بيروحوا). موثّق بالتفصيل بـ `README.md`.

---

## 2. وين وصلنا بالتعديلات (حالة المشروع الفعلية)

### المرحلة 1 — الأساس ✅ مكتملة
- تسجيل دخول/خروج (Sanctum)، صلاحيات وأدوار (owner/doctor/secretary/accountant)
- مرضى: كود تلقائي، طفل/بالغ، ملاحظات، مرفقات، **زيارة walk-in** (تضيف مريض وتبلش تشتغل عليه فوراً بدون حجز موعد)
- رسمة أسنان تفاعلية بترقيم FDI كامل (دائم + لبني)، **أشكال أسنان واقعية** (مو أرقام بمربعات) قابلة للنقر، مبنية حسب صورة مرجعية زودتنا فيها
- أطباء (تعاقدات + دوام + عمولات)، خدمات (تصنيفات + أسعار فروع)
- مواعيد: تقويم يومي بصري، توليد فراغات تلقائي، كشف تعارض
- **تاريخ dd/mm/yyyy صارم بكل مكان** عبر تقويم مخصص (`DatePicker.tsx`) — ما في ولا `<input type="date">` أصلي بكل المشروع (كانت هاي مشكلة اشتكيت منها وانحلت بالكامل)
- نسخ احتياطي واستعادة كاملة من الواجهة (`pg_dump`/`pg_restore`)

### المرحلة 2 — العلاج والمال ✅ مكتملة
- خطط علاج: بنود (خدمة + سن + سطوح + سعر + جلسات) → **اعتماد الخطة بس يجدول الجلسات، ما بترتب أي دين فوراً** (انعدّل 21/07/2026 — راجع "آخر تحديث" تحت) → زر "جدولة الجلسات" يحجز مواعيد تلقائياً حسب الفترة بين كل جلسة → كل جلسة تُحاسب لحالها لما تصير فعلياً (بسعر قابل للتعديل + خصم اختياري + دفع فوري أو دين)
- كشف حساب موحّد لكل مريض بالشيكل، تحصيل دفعات بأي عملة (بتنحسب على صندوق العملة وتنعكس بالشيكل بالكشف)
- صناديق مستقلة لكل عملة/فرع، مصاريف ووارد بتصنيفات
- عمولات الأطباء تُحسب تلقائياً لما تعلّم إجراء "منجز"، مع كشف تسوية شهري لكل طبيب

### المرحلة 3 — الموردون والشيكات والمشتريات ✅ مكتملة
- موردين: كشف حساب (رصيد متراكم) + تحصيل/دفع
- أصناف ومخزون: تصنيفات + أصناف (عادي / مخزون بسيط / تتبّع دفعات وصلاحية — متل الزرعات)
- فواتير شراء: مسودة → إضافة بنود → تأكيد (بيحرك المخزون فعلياً وينشئ دفعات/لوتات تلقائياً)
- شيكات: استلام (من مريض أو لمورد) → تظهير / رجوع / تحصيل، بحالات ملوّنة وربط تلقائي بكشوفات الحسابات والصناديق

كل المراحل التلاتة **مجرّبة يدوياً بالمتصفح الفعلي**، مو بس بالكود.

### آخر إصلاح
لقينا خلل بصفحة الشيكات — عمود "الاستحقاق" كان يعرض التاريخ خام (`2026-08-19T00:00:00.000000Z`) بدل `19/08/2026`. انصلح فوراً.

### تحديث 21-22/07/2026 — أداء + إعادة تصميم منطق الفوترة + صفحة المريض

**الأداء:**
- OPcache كان **معطّل بالكامل** بـ`php.ini` — هاد كان السبب الرئيسي لبطء النظام (كل طلب كان يعيد تفسير كل ملفات لارافيل من الصفر). تفعيله خفّض زمن الطلب من ~420ms لـ~37ms (تحسّن 11 ضعف). انعمل سكريبت `enable-opcache.bat` يشتغل تلقائياً مع `check-requirements.bat`/`start.bat` بكل جهاز (لأنه `php83/` مو مرفوع على Git، فكل جهاز محتاج التفعيل هذا لحاله).

**إعادة تصميم منطق فوترة خطط العلاج (تغيير جوهري):**
كان اعتماد خطة علاج متعددة الجلسات يسجّل **دين كامل الخطة فوراً** لحظة الاعتماد، قبل ما توصل حتى جلسة وحدة — هاد كان غلط بمنطق العمل الفعلي (الدفع/الدين المفروض يكون *لما الجلسة تصير فعلياً*، مش مسبقاً). انعدّل بالكامل:
- اعتماد الخطة الآن بس يجدول الجلسات (`TreatmentPlanService::approve()`) — صفر أثر مالي.
- كل جلسة تُحاسب لحالها بـ`completeSession()` — بسعر قابل للتعديل وقتها + خصم اختياري + خيار دفع فوري (يختار الصندوق) أو تركها دين. هاد بينطبق أوتوماتيكياً على زيارة "اجاني هلق" (زيارة يوم واحد = جلسة وحدة تتحاسب فوراً بنفس الآلية).
- كل جلسة قابلة للتعديل لاحقاً (سعر/ملاحظة عبر `updateSession()` — الفرق بينسجل كتسوية `adjustment` بكشف الحساب، مو كتعديل صامت للسجل الأصلي) أو الإلغاء (`cancelSession()` — بيرد قيمتها بالظبط، بيرجّع لون السن، وبيلغي عمولة الطبيب غير المسوّاة إذا كانت مرتبطة).
- إلغاء خطة كاملة أو بند كامل بيلغي كل جلساتها الغير محسوبة، وبيحترم الجلسات المحسوبة/المدفوعة (ما بيلغيها تلقائياً).
- **Migration جديدة مهمة**: `invoice_lines` صار فيها `plan_item_session_id` (تربط كل سطر فاتورة بالجلسة المحددة اللي ولّدته) — أساس كل منطق التعديل/الإلغاء الدقيق فوق.

**إصلاح: بطاقة "أرصدة المرضى المستحقة" بالداشبورد كانت غلط** — كانت تجمع أرصدة كل المرضى مع بعض بمجموع واحد عام، فلو مريض إله رصيد لصالحه (credit) كان يقلّل من مجموع دين مريض تاني وبالتالي يخفي دين حقيقي. انصلحت تجمع بس الأرصدة المدينة فعلياً لكل مريض لحاله (نفس منطق صفحة "الديون").

**صفحة المريض — إعادة تنظيم كاملة:**
- صارت تبويبات (العلاج | سجل الزيارات | الحساب | المواعيد | الملاحظات والمرفقات) بدل ما تكون كل الأقسام مرصوصة تحت بعض بارتفاعها الكامل — الصفحة صارت أقصر بكثير.
- **تبويب جديد "سجل الزيارات"**: قائمة بكل زيارة محسوبة، الضغط عليها يفتحها وتقدر: تعدّل السعر، تكتب/تعدّل ملاحظة، تحصّل دفعة مباشرة، أو تحذفها بالكامل (استرجاع تلقائي).
- رسمة الأسنان: سجل كل سن صار قابل للتعديل والحذف (كان قراءة فقط)، وضيف checkbox "اشتغل عليه طرف خارجي" (يبيّن بخط منقّط مميز عالرسمة) وربط finding بجلسة فعلية موجودة إذا ما كانت مربوطة أصلاً.
- الملاحظات العامة للمريض صارت قابلة للتعديل (مو بس إضافة/حذف).

كل هالتغييرات **مجرّبة فعلياً** — منطق الفوترة عبر psql/tinker end-to-end (اعتماد → حساب جلسة → تعديل سعر → حذف، مع التحقق من كشف الحساب/الفاتورة/الصندوق بكل خطوة)، والواجهة عبر `tsc`/`vite build` نظاف. مرفوعة على branch `ui-redesign-dashboard-telegram`.

> **تنويه مهم لأي جهاز يسحب هالتحديث**: فيه 3 migrations جديدة، لازم `php83\php.exe backend\artisan migrate` بعد أي `git pull`.

---

## 3. الخطوات القادمة (حسب الخطة الأصلية)

### المرحلة 4 — بوت تيليغرام
- ربط حساب مستخدم بحساب تيليغرام (`telegram_links` — الجدول مبني من المرحلة 1 بس مو مفعّل)
- تنبيهات مواعيد، تذكير بصور شيكات مستحقة
- أوامر سريعة من البوت (حجز، استعلام كشف حساب...)

### المرحلة 5 — جاهزية SaaS
- تفعيل نظام الاشتراكات والخطط (`plans`, `subscriptions`, `plan_features` — مبنية من المرحلة 1 بس مو مفعّلة منطقياً)
- تفعيل تعدد العيادات (multi-tenancy) بشكل كامل — البنية جاهزة من البداية بس شغالة حالياً بعيادة وحدة ثابتة (`clinic_id = 1`)
- بوابة ميزات (`feature()`) فعلياً تفرّق بين الخطط بدل ما ترجع `true` دايماً

### مهام إضافية مفتوحة (مو جزء من مرحلة رسمية)
- تأمين صحي (`insurers`, `patient_insurer`) — الجداول موثقة بـ `docs/erd.md` بس مو منفذة، ومو ضمن نطاق أي مرحلة حالياً
- مراجعة تصميم/UX لصفحات المرحلة 3 (الموردون، الأصناف، فواتير الشراء، الشيكات) — طلبتوا نمشي عالصفحات سوا نعدّلها، هاد لسا ما بلشناه

> **قاعدة العمل المتفق عليها**: كل مرحلة تنبني وتتوقف للمراجعة والتأكيد قبل ما نكمل للي بعدها. حالياً متوقفين بعد المرحلة 3 بانتظار توجيهك.

---

## 4. بنية المجلد (مرجع سريع)
```
DentaFlow/
├── backend/          Laravel 13 API (PHP 8.3، PostgreSQL 18)
├── frontend/         React + Vite + TypeScript + Tailwind (واجهة عربية RTL)
├── docs/erd.md        مخطط كل الجداول لكل المراحل الخمس (مرجعي، حتى الغير منفذ بعد)
├── php83/             PHP محلي (غير مرفوع على Git — حمّله بنفسك)
└── composer.phar       Composer محلي (غير مرفوع على Git — حمّله بنفسك)
```

راجع `README.md` للتفاصيل التقنية الكاملة (القواعد المعمارية، النسخ الاحتياطي، الأخطاء المكتشفة وحلولها).
